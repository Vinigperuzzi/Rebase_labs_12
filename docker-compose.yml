version: '3.8'

services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: rebase_labs
      POSTGRES_PASSWORD: labs_123
      POSTGRES_DB: labs_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    depends_on:
      - db

volumes:
  postgres_data:
