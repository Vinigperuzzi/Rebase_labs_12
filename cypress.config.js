module.exports = {
  e2e: {
    baseUrl: 'http://localhost:3000', // URL do seu app
    supportFile: false, // Se não quiser usar suporte customizado
    setupNodeEvents(on, config) {
      // Eventos de configuração adicionais
    },
  },
};